#include <stdio.h>
int main()
{
    int n,sum=0;
    
    
    do
    {
       printf("enter a number(0 to exit)");
     scanf("%d",&n);
     sum=sum+n;
    }
while(n!=0);
{
    printf("the sum  is %d",sum);
    
}
}
